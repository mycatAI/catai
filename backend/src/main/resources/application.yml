server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/catai?createDatabaseIfNotExist=true&useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

app:
  jwt:
    secret: cataiPetDiary2024SecretKeyForJWTTokenGenerationAndValidationMustBeLongEnough256Bits!!
    expiration-ms: 604800000
